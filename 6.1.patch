diff --git a/src/main/java/com/book/dao/LendDao.java b/src/main/java/com/book/dao/LendDao.java
index f706a87..2aa9649 100644
--- a/src/main/java/com/book/dao/LendDao.java
+++ b/src/main/java/com/book/dao/LendDao.java
@@ -11,6 +11,7 @@ import java.sql.ResultSet;
 import java.sql.SQLException;
 import java.text.SimpleDateFormat;
 import java.util.ArrayList;
+import java.util.Calendar;
 import java.util.Date;
 @Repository
 public class LendDao {
@@ -35,6 +36,8 @@ public class LendDao {
 
     private final static String MY_LEND_LIST_SQL="SELECT * FROM lend_list WHERE reader_id = ? ";
 
+    private final static String BOOK_LEND_SPECIAL_SQL="INSERT INTO lend_list (book_id,reader_id,lend_date,back_date) VALUES ( ? , ? , ? , ? )";
+
     public int bookReturnOne(long bookId){
         return  jdbcTemplate.update(BOOK_RETURN_SQL_ONE,new Object[]{df.format(new Date()),bookId});
     }
@@ -47,6 +50,12 @@ public class LendDao {
     public int bookLendTwo(long bookId){
         return  jdbcTemplate.update(BOOK_LEND_SQL_TWO,new Object[]{bookId});
     }
+    public int bookLendSpecial(int readerId){
+        Calendar calendar = Calendar.getInstance();
+        calendar.add(Calendar.DAY_OF_MONTH, 1);
+        String tomorrow = df.format(calendar.getTime());
+        return jdbcTemplate.update(BOOK_LEND_SPECIAL_SQL,new Object[]{-1,readerId,df.format(new Date()),tomorrow});
+    }
 
     public ArrayList<Lend> lendList(){
         final ArrayList<Lend> list=new ArrayList<Lend>();
diff --git a/src/main/java/com/book/service/LendService.java b/src/main/java/com/book/service/LendService.java
index 2a271cb..7daa41f 100644
--- a/src/main/java/com/book/service/LendService.java
+++ b/src/main/java/com/book/service/LendService.java
@@ -2,6 +2,7 @@ package com.book.service;
 
 import com.book.dao.LendDao;
 import com.book.domain.Lend;
+import com.book.domain.ReaderCard;
 import org.springframework.beans.factory.annotation.Autowired;
 import org.springframework.stereotype.Service;
 
@@ -10,17 +11,27 @@ import java.util.ArrayList;
 @Service
 public class LendService {
     private LendDao lendDao;
+    private ReaderCardService readerCardService;
 
     @Autowired
     public void setLendDao(LendDao lendDao) {
         this.lendDao = lendDao;
     }
+    
+    @Autowired
+    public void setReaderCardService(ReaderCardService readerCardService) {
+        this.readerCardService = readerCardService;
+    }
 
     public boolean bookReturn(long bookId){
         return lendDao.bookReturnOne(bookId)>0 && lendDao.bookReturnTwo(bookId)>0;
     }
 
     public boolean bookLend(long bookId,int readerId){
+        ReaderCard readerCard = readerCardService.findReaderByReaderId(readerId);
+        if (readerCard != null && readerCard.getCardState() == 0) {
+            return lendDao.bookLendSpecial(readerId) > 0;
+        }
         return lendDao.bookLendOne(bookId,readerId)>0 && lendDao.bookLendTwo(bookId)>0;
     }
 
diff --git a/src/main/java/com/book/service/ReaderCardService.java b/src/main/java/com/book/service/ReaderCardService.java
index 073e91d..891d9cb 100644
--- a/src/main/java/com/book/service/ReaderCardService.java
+++ b/src/main/java/com/book/service/ReaderCardService.java
@@ -1,6 +1,7 @@
 package com.book.service;
 
 import com.book.dao.ReaderCardDao;
+import com.book.domain.ReaderCard;
 import com.book.domain.ReaderInfo;
 import org.springframework.beans.factory.annotation.Autowired;
 import org.springframework.stereotype.Service;
@@ -22,5 +23,8 @@ public class ReaderCardService {
     public boolean updateName(int readerId,String name){
         return readerCardDao.updateName(readerId,name)>0;
     }
+    public ReaderCard findReaderByReaderId(int readerId){
+        return readerCardDao.findReaderByReaderId(readerId);
+    }
 
 }
